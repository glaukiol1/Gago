package parser

// handle expressions after all the filtering and whtiespace removal, handle the return codes
// from the parser.checkPattern() function

func handlePattern(cursor *multipleCursor, code int) {
	switch code {

	}
}
